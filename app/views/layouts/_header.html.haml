%nav.navbar.navbar-inverse.navbar-fixed-top{:role => "navigation"}
  .container-fluid
    .navbar-header
      %button.navbar-toggle.collapsed{"data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{href: "/"} Mi Peluquería
    #bs-example-navbar-collapse-1.collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li{class: ('active' if current_page?(places_path))}
          = link_to places_path, class: '' do
            %i.fa.fa-building-o
            =t(:places)
        %li{class: ('active' if current_page?(bookings_path))}
          = link_to bookings_path, class: '' do
            %i.fa.fa-calendar
            =t(:bookings)
            %span.badge.badge-success
              =pending_confirm_count
        %li{class: ('active' if current_page?(users_path))}
          = link_to users_path, class: '' do
            %i.fa.fa-users
            =t(:users)
      %ul.nav.navbar-nav.navbar-right
        %li
          = link_to destroy_admin_session_path, method: :delete, class: '' do
            %i.fa.fa-user
            =t(:sign_out)
