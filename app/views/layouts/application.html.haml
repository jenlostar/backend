!!!
%html
  %head
    %meta{:charset => "utf-8"}
    %meta{:content => "IE=edge", "http-equiv" => "X-UA-Compatible"}
    %meta{:content => 'width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no', :name => 'viewport'}
    %meta{:name => "google", :value => "notranslate"}
    %title Backend
    = stylesheet_link_tag    'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
  %body
    / fixed top navbar
    .navbar.navbar-default.navbar-fixed-top{:role => "navigation"}
      .container
        .navbar-header
          %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", :type => "button"}
            %span.sr-only Navegación alterna
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
        %button.btn.btn-default.btn-back.pull-left.hidden{:onclick => "history.back()", :type => "button"}
          %i.fa.fa-lg.fa-chevron-left
          %span Atras
        / menu button to show/ hide the off canvas menu
        %button.btn.btn-default.btn-menu.pull-left{"data-toggle" => "offcanvas", :type => "button"}
          %i.fa.fa-lg.fa-bars
          %span Menu
        %a.navbar-brand{:href => "/", :title => "Backend"} Backend
        / navbar menu options: shown on desktop only
        .navbar-collapse.collapse
          %ul.nav.navbar-nav
            %li
              %a{:href => "#"}
                %i.fa.fa-dashboard
                Dashboard
            %li.active
              %a{:href => places_path}
                %i.fa.fa-building-o
                =t(:places)
    / slide in off canvas menu (mobile only)
    %nav.navmenu.offcanvas.offcanvas-left
      %ul.nav
        %li
          %a{:href => "#"}
            %i.fa.fa-lg.fa-dashboard
            Dashboard
        %li.active
          %a{:href => "starter-template.html"}
            %i.fa.fa-lg.fa-building-o
            =t (:places)
    / nav
    .container.bootcards-container
      - flash.each do |name, msg|
        .alert{:class => "alert-#{name}", :role => 'alert'}= msg
      = yield
    :javascript
      /*
       * Initialize Bootcards.
       *
       * Parameters:
       * - offCanvasHideOnMainClick (boolean): hide the offcanvas menu on clicking outside the off canvas
       * - enableTabletPortraitMode (boolean): enable single pane mode for tablets in portraitmode
       * - disableRubberBanding (boolean): disable the iOS rubber banding effect
       */
      bootcards.init( {
        offCanvasHideOnMainClick : true,
        enableTabletPortraitMode : true,
        disableRubberBanding : true
      });

      //enable FastClick
      window.addEventListener('load', function() {
          FastClick.attach(document.body);
      }, false);
